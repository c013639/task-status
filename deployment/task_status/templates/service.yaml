apiVersion: v1
kind: Service
metadata:
  name: {{ include "task_status.fullname" . }}
  labels:
    {{- include "task_status.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "task_status.selectorLabels" . | nindent 4 }}
